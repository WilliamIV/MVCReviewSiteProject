@model IEnumerable<MVCReviewSite.Models.Review>

@{
    ViewBag.Title = "Index";
}

<h2 id="ResortName">Sandals Luxury Carribbean Resort</h2>
<!-- 16:9 aspect ratio -->
<div class="moviewrapper">
    <iframe width="640" height="360" src="https://www.youtube.com/embed/996UrPUMB8s?autoplay=1" frameborder="0" allowfullscreen></iframe>
</div>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<div class="row">

    @*@Html.DisplayNameFor(model => model.Resort.Name)

        @Html.DisplayNameFor(model => model.Name)

        @Html.DisplayNameFor(model => model.Email)

        @Html.DisplayNameFor(model => model.TravelDate)

        @Html.DisplayNameFor(model => model.Comment)*@


    @foreach (var item in Model)
    {

        <div class="col-sm-6 col-md-4">
            <div class="thumbnail">
                <div class="caption">
                    <h3>@Html.DisplayFor(modelItem => item.Resort.Name)</h3>
                    <p>@Html.DisplayFor(modelItem => item.TravelDate)</p>
                    <p>@Html.DisplayFor(modelItem => item.Comment)</p>
                    <p>
                        @Html.ActionLink("Details", "Details", new { id = item.ID }, new { @class = "btn btn-primary" })
                    @*@Html.DisplayFor(modelItem => item.Resort.Name)

                        @Html.DisplayFor(modelItem => item.Name)

                        @Html.DisplayFor(modelItem => item.Email)

                        @Html.DisplayFor(modelItem => item.TravelDate)

                        @Html.DisplayFor(modelItem => item.Comment)

                        @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
                        @Html.ActionLink("Details", "Details", new { id=item.ID }) |
                        @Html.ActionLink("Delete", "Delete", new { id=item.ID })*@

                    

            </div>
                </div>
            </div>
   }
